# High Level Synthesis Analysis Code base

This repository serves as introductory code base for analysis of high level synthesis code

# Build instruction

Run the following to build the codebase

```sh
$ bash ./install_circt.sh
$ bash ./build.sh
```